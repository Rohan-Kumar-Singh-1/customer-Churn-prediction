FROM node:20-alpine

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json ./

RUN npm install

COPY frontend/ .

RUN npm run build

# Serve using lightweight server
RUN npm install -g serve

EXPOSE 3000

CMD ["sh", "-c", "serve -s dist -l $PORT"]
